import { LazyLoadTextDirective } from './lazy-load-text.directive';

describe('LazyLoadTextDirective', () => {
  it('should create an instance', () => {
    const directive = new LazyLoadTextDirective();
    expect(directive).toBeTruthy();
  });
});
