import { ProgressCircleAnimationDirective } from './progress-circle-animation.directive';

describe('ProgressCircleAnimationDirective', () => {
  it('should create an instance', () => {
    const directive = new ProgressCircleAnimationDirective();
    expect(directive).toBeTruthy();
  });
});
