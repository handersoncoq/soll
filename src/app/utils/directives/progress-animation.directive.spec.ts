import { ProgressAnimationDirective } from './progress-animation.directive';

describe('ProgressAnimationDirective', () => {
  it('should create an instance', () => {
    const directive = new ProgressAnimationDirective();
    expect(directive).toBeTruthy();
  });
});
